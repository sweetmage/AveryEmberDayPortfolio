# Gemini — Agent Pointer

> **Canonical agent docs live in [`AGENTS.md`](./AGENTS.md).** Read that first.

This file exists so Gemini (or any agent routing through Gemini models) finds project-specific instructions when opening this repo.

## Gemini-specific notes
- Gemini models are configured as Blackbox providers in `~/.config/kilo/kilo.jsonc`.
- When updating global conventions that affect this repo, mirror changes into `AGENTS.md`.
