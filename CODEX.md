# Codex — Agent Pointer

> **Canonical agent docs live in [`AGENTS.md`](./AGENTS.md).** Read that first.

This file exists so Codex finds project-specific instructions when opening this repo.

## Codex-specific notes
- Codex global config lives at `~/.codex/AGENTS.md` and `~/.codex/config.toml`.
- When updating global conventions that affect this repo, mirror changes into `AGENTS.md`.
