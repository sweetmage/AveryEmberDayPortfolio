<!-- shxdowmap: built=2026-08-11T01:21:45Z fingerprint=14cd811fae5e32aa660c4f8edd1e99d4d962d063 handle=vesper -->
# Avery Ember Day Portfolio - Architecture

> Source-of-truth map for agents. Prefer this over re-reading the tree.
> Operational rules (branch policy, deploy gotchas, build commands, design
> conventions) live in [`../AGENTS.md`](../AGENTS.md) and are **not** duplicated
> here - this file describes *structure*, AGENTS.md describes *process*.

## Overview

A personal portfolio site for a brand and visual designer. It is a **Next.js 15
static export** (`output: 'export'`) - there is no server at runtime, no API, and
no database. `next build` emits plain HTML/CSS/JS into `out/`, and Netlify
publishes that directory.

Two sites live in this one repo, and confusing them is the single most common
mistake here:

1. **The Next.js app in [`app/`](../app/)** - this is what deploys and what
   matters. App Router, five routes, authored in Tailwind v4 utility classes.
2. **A legacy hand-written static site** at the repo root
   ([`index.html`](../index.html), [`gallery/`](../gallery/),
   [`projects/*.html`](../projects/)) - retained, still tracked, **not deployed**.
   It is the only consumer of the committed [`style.css`](../style.css).

```
                    ┌──────────────────────────────────────────┐
  source            │ app/            brand.css   src/css/     │
                    │ (App Router)    (tokens)    (reset,      │
                    │                             preset)      │
                    └───────┬──────────────────────┬───────────┘
                            │                      │
              app/globals.css                  app.css
              (Next PostCSS)                (Tailwind CLI)
                            │                      │
                            v                      v
                    ┌───────────────┐      ┌──────────────┐
                    │ next build    │      │ style.css    │
                    │   -> out/     │      │ (committed)  │
                    └───────┬───────┘      └──────┬───────┘
                            │                     │
                            v                     v
                    ┌───────────────┐      ┌──────────────────┐
                    │ Netlify       │      │ legacy root site │
                    │ (publish=out) │      │ NOT deployed     │
                    └───────────────┘      └──────────────────┘

  runtime (browser, no server):
    theme-init.js  -> stamps data-theme on <html> before paint
    bubbles.js     -> DOM physics engine, exclusion zones

  the Mistrust case study is React, not a runtime script:
    MistrustSlideshow.tsx -> swipeable stage, Set 1/2/3 switcher, filmstrip
    useSwipeDeck.ts       -> pointer/keyboard deck state
    MistrustLightbox.tsx  -> fullscreen viewer
    SlideGrid.tsx         -> the All Slides mosaics
    mistrustSlides.ts     -> slide manifest + SLIDE_ALT
```

**Two CSS entry points compile the same sources.** `app/globals.css` (Next's
PostCSS pipeline) governs what deploys; `app.css` (Tailwind CLI) produces the
committed `style.css` for the legacy site. Both import `brand.css` into the
`components` layer so utilities can override it. Editing one and not the other is
a real failure mode.

## Repo / Module Map

<!-- shxdowmap:begin:module-map -->
_Generated by `shxdowmap refresh --auto`; edits inside this block are overwritten. Agent-owned prose belongs outside the markers._

| Path | Code files | Languages |
|---|---|---|
| `.` | 3 | ts js mjs |
| `app` | 4 | tsx ts |
| `app/components` | 8 | tsx ts |
| `app/contact` | 1 | tsx |
| `app/contact/thanks` | 1 | tsx |
| `app/gallery` | 3 | tsx ts |
| `app/projects` | 9 | tsx ts |
| `public/scripts` | 2 | js |
| `scripts` | 13 | js |
| `scripts/_archive` | 1 | js |
| `tests` | 12 | js |

<!-- shxdowmap:modules: . app public scripts tests -->
<!-- shxdowmap:end:module-map -->

The `tmp/` and `output/playwright/` rows that used to appear in that table are
gone: 18 tracked scratch files (one-off Python screenshot/inspection scripts, a
spawn test, stale submenu PNGs from a nav that no longer exists, plus root-level
`Script.js` and `run_git_commands.py`) were untracked on 2026-08-03 at the user's
instruction and are now gitignored.

**One correction, 2026-08-09 (Entry 128).** This paragraph used to end "They still
exist on the author's disk; they are simply no longer part of the repo. Nothing
imported them." Both halves were wrong about `Script.js`: it is gone from disk
too, and four legacy pages still had `<script src="Script.js">` tags, so every
load of those pages 404ed. That failure sat red in the suite for six days behind
`smoke-interaction.spec.js`, whose diagnosis nobody read. The tags are gone now
and the spec is deleted. Everything else in the batch really was unimported.

Roles, for the directories that carry real weight:

| Path | Role |
|---|---|
| [`app/`](../app/) | The deployed Next.js App Router site. Five routes: `/`, `/projects/`, `/gallery/`, `/contact/`, `/contact/thanks/`. |
| [`app/components/`](../app/components/) | Shared chrome: `Nav`, `Footer`, `SkipLink`, `ReturnToTop`, `BubbleLogo`, `BubblePhysics`, `ConnectLinks`. |
| [`app/projects/`](../app/projects/) | Projects page. `ProjectTabs` drives a sticky vertical rail at `lg+`; `BrandProject` / `MistrustProject` are the two case-study panels. |
| [`app/gallery/`](../app/gallery/) | Gallery page. `gallery-data.ts` is the item list (tags, tools, descriptions); `GalleryGrid` is the client filter + grid. |
| [`brand.css`](../brand.css) | **Design-system source of truth.** All `--brand-*` tokens, keyframes, and component visuals. Imported into the `components` cascade layer - never linked directly. |
| [`src/css/`](../src/css/) | `site.css` (reset, base type, logo theme-swap, `#return-to-top`) and `tailwind-preset.css` (bridges `--brand-*` tokens to Tailwind theme names). |
| [`public/scripts/`](../public/scripts/) | The runtime JS the export actually serves: `bubbles.js`, `theme-init.js`. (`history-of-mistrust-slideshow.js` lived here until Entry 109 replaced it with React components.) |
| [`scripts/`](../scripts/) | Node maintenance tooling: asset generation, measurement gates, TickTick/Google sync. Not shipped. |
| [`tests/`](../tests/) | Playwright suite: smoke, visual-baseline gate, bubble-engine coverage, Mistrust set-strip checks, and `nav-safari.spec.js` under a separate **`webkit-mobile`** project (the chromium project ignores that file — it guards a WebKit-only layout bug Chromium cannot reproduce). Everything here tests the Next.js export on :4322; the legacy root site is no longer tested. |
| [`images/`](../images/) vs [`public/images/`](../public/images/) | **Two trees, deliberately NOT mirrors.** `images/` serves the legacy root site and holds the **sources** (Figma PNG exports, moodboards, working notes). `public/images/` is what Next copies into the export and holds **derived outputs only** — everything there is published, so a source file left in it is dead weight on every deploy (6 MB of it was, until Entry 129). Generators read from `images/` and write outputs to both. Do not "restore symmetry" between them. |
| [`docs/`](../docs/) | `NOTES.md`, `deploys.md`, `accessibility.md`, `plans/` (**open plans only** — the 23 finished ones were archived 2026-08-09; plus a `README.md` index), `archives/` (`plans.md`, the consolidated plan archive), `checkpoints/`. |

**Where does X live?**

- Nav links, and the Contact-link enable/disable: [`app/components/Nav.tsx`](../app/components/Nav.tsx) + [`app/components/Footer.tsx`](../app/components/Footer.tsx).
- Colors, spacing, radii, the content max-width: [`brand.css`](../brand.css) `:root`.
- Gallery items, tags, tool lists: [`app/gallery/gallery-data.ts`](../app/gallery/gallery-data.ts).
- Slide alt text and the slide manifest for the Mistrust case study: `SLIDE_ALT` in [`app/projects/mistrustSlides.ts`](../app/projects/mistrustSlides.ts).
- Social share card descriptor: [`app/og.ts`](../app/og.ts) (one shared object, all four pages).
- Production headers/CSP: [`netlify.toml`](../netlify.toml). Dev-only headers: [`next.config.ts`](../next.config.ts).

## Execution Model

There is no server. Everything is a build step or browser runtime.

| Command | What it does |
|---|---|
| `npm run dev` | `next dev` on :3000. The real app, hot reload. **The dev/preview surface during the deploy pause.** |
| `npm run build:next` | `next build` -> static export into `out/`. |
| `npm run css:build` | Tailwind CLI, `app.css` -> `style.css` (minified). Legacy site only. Commit the rebuilt file. |
| `npm test` | Playwright: 173 tests as of 2026-08-14 (40 visual baselines + 32 gallery-expand + 25 Mistrust slideshow + 22 bubble + 21 webkit-mobile nav + 18 sticky-chrome + 8 set-strip + 4 smoke-next + 3 mobile-zoom). Verify with `--list`; this number goes stale. |
| `npm run serve` | `serve . -l 8080` - **legacy root site only**, not the Next app. |

**Build ordering trap:** `distDir` is `out`, so `build:next` deletes and recreates
the directory a running `next dev` is serving from. Never run them concurrently -
every route starts 500ing with `ENOENT .../out/routes-manifest.json`.

**Browser runtime**, all in [`public/scripts/`](../public/scripts/):

- `theme-init.js` - loaded via `<Script strategy="beforeInteractive">` from
  [`app/layout.tsx`](../app/layout.tsx). Stamps `data-theme` on `<html>` before
  first paint so there is no theme flash. It **must render inside `<body>`**; as
  a direct child of `<html>` React throws a hydration error on every load.
- `bubbles.js` - DOM-based physics engine. Bubbles avoid *exclusion zones*
  resolved by CSS selector from `DEFAULT_EXCLUSIONS` (includes the semantic
  `.bubble-exclude` marker), `HOME_EXCLUSIONS`, and per-page `data-exclusions`.
  Exposes `window.__bubbleEngine` for tests. **Duplicated** at
  [`scripts/bubbles.js`](../scripts/bubbles.js); `public/` is the served copy.
The Mistrust case study used to be a third runtime script
(`history-of-mistrust-slideshow.js`, a carousel plus lightbox). Entry 109 replaced
it with React client components under [`app/projects/`](../app/projects/):
`MistrustSlideshow.tsx` (swipeable stage, Set 1/2/3 switcher, filmstrip and
side-bar nav) on [`useSwipeDeck.ts`](../app/projects/useSwipeDeck.ts),
`MistrustLightbox.tsx`, `SlideGrid.tsx` for the All Slides mosaics, and
[`mistrustSlides.ts`](../app/projects/mistrustSlides.ts) for the manifest. The old
script is deleted; nothing serves a slideshow from `public/scripts/` anymore.

**Deploy flow:** push `portfoliowebsite` -> Netlify runs `next build` -> publishes
`out/`. A docs-only push is cancelled by the `[build] ignore` rule in
`netlify.toml`. Deploys are **credit-metered, not minute-metered** (15 credits per
production deploy, 300/month). Full rules: [`deploys.md`](deploys.md).

## Data / Config Model

No database, no ORM, no migrations. "Data" is TypeScript literals and image trees.

| Store | Location | Shape |
|---|---|---|
| Gallery items | [`app/gallery/gallery-data.ts`](../app/gallery/gallery-data.ts) | `GalleryItem[]` = `{ src, alt, caption, width, height, tags, tools, description }`. `tags` (Digital/Traditional) drive the filter and render `sr-only`; `tools` is the visible middot-separated line; `description` is `''` on every item and awaits the user's copy pass. The render path for it **exists** as of Entry 118 - `GalleryGrid` shows it clamped to one line on a collapsed card and in full on an expanded one, but only when the string is non-empty, so filling these in needs no code change. |
| Project case studies | [`app/projects/BrandProject.tsx`](../app/projects/BrandProject.tsx), [`MistrustProject.tsx`](../app/projects/MistrustProject.tsx) | JSX, not data. No tag system on this page. |
| Slide captions | `SLIDE_ALT` in [`app/projects/mistrustSlides.ts`](../app/projects/mistrustSlides.ts) | 30-entry array. Feeds both `<img alt>` and lightbox captions. Set title cards must land on indices 1 / 11 / 21 for the `Math.ceil(n / 10)` set math. Twelve entries were found misordered against the artwork in Entry 106 - verify against the images, not the order. |
| Mistrust set strips | [`scripts/generate-mistrust-assets.js`](../scripts/generate-mistrust-assets.js) | Composes `set-N.webp` taking **pixels from the slide PNGs** and **geometry from [`frame-geometry.json`](../images/myart/A%20History%20of%20Mistrust/frame-geometry.json)**, the committed manifest of Figma canvas coordinates, because slides 1 and 2 share a 19px band that naive cumulative-width layout draws twice (Entry 114). Geometry came from the raster `Set N.png` exports until 2026-08-14. Guarded by dimension, gap and ordering assertions plus `tests/mistrust-sets.spec.js`. |
| Share card | [`app/og.ts`](../app/og.ts) | One `{ url, width, height, alt }` object consumed by all four pages. |

**Config files:** [`next.config.ts`](../next.config.ts) (export mode, dev-only
headers), [`netlify.toml`](../netlify.toml) (build, production CSP/headers,
redirects, ignore rule), [`playwright.config.js`](../playwright.config.js),
[`tsconfig.json`](../tsconfig.json), [`postcss.config.mjs`](../postcss.config.mjs).

**Secrets** live in a gitignored `.env` at the repo root. Load with
`import 'dotenv/config'`. Variables (all for maintenance scripts, none reach the
browser): `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `GOOGLE_REDIRECT_URI`,
`GOOGLE_TOKEN_URI`, `GOOGLE_REFRESH_TOKEN`, `GOOGLE_ACCESS_TOKEN`,
`TICKTICK_ACCESS_TOKEN`. The Google access token is short-lived - refresh it from
`GOOGLE_REFRESH_TOKEN` rather than assuming it works.

## Dependencies & Manifests

<!-- shxdowmap:begin:dependencies -->
_Generated by `shxdowmap refresh --auto`; edits inside this block are overwritten. Agent-owned prose belongs outside the markers._

| Manifest | Ecosystem |
|---|---|
| `package.json` | Node/Deno |

_Declared manifests only; lockfiles are intentionally excluded (transitive churn is not an architecture change)._
<!-- shxdowmap:end:dependencies -->

Notes on the pins that matter:

- **Tailwind v4** (`@tailwindcss/cli` + `@tailwindcss/postcss`). v4 uses
  `@import "tailwindcss"` and `@source` directives, not a `tailwind.config.js`.
  The `@source not "**/*.md"` exclusions are load-bearing: without them the class
  scanner reads `LOGBOOK.md`/`TODO.md` and a class merely *mentioned* in prose is
  compiled back into the shipped CSS.
- **`sharp`** is used only by the generator scripts (webp encoding, og downsample),
  never at runtime.
- **`serve`** backs both `npm run serve` (legacy, :8080) and the Playwright test
  server (:4322, started in global setup).
- **Playwright** is pinned off Netlify with `PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD=1` -
  CI never runs the suite. Adding a test step to the build command means dropping
  that variable first.
- The lockfile is committed; `node_modules/` is not.

## Key Files & Entry Points

<!-- shxdowmap:begin:key-files -->
_Generated by `shxdowmap refresh --auto`; edits inside this block are overwritten. Agent-owned prose belongs outside the markers._

**Manifests / build files**

- `package.json`

**Entry-point candidates** (path heuristic, depth <= 3)

- (none matched)
<!-- shxdowmap:end:key-files -->

The anti-grep shortcut - open these first, by task:

| Task | Start here |
|---|---|
| Anything visual | [`brand.css`](../brand.css) (tokens + components), then the page's `.tsx`. |
| Layout / alignment | [`brand.css`](../brand.css) `--brand-content-max`, [`src/css/site.css`](../src/css/site.css) `main`, [`app/PageHeader.tsx`](../app/PageHeader.tsx). Verify with `node scripts/measure-content-widths.js`. |
| Nav or theme toggle | [`app/components/Nav.tsx`](../app/components/Nav.tsx), then `.brand-nav*` / `#theme-toggle` in `brand.css`. |
| Bubble behavior | [`public/scripts/bubbles.js`](../public/scripts/bubbles.js) (`DEFAULT_EXCLUSIONS`), [`tests/bubbles-exclusion.spec.js`](../tests/bubbles-exclusion.spec.js). |
| Page metadata / unfurls | [`app/og.ts`](../app/og.ts) + the `metadata` export in each `page.tsx`. `metadataBase` is in [`app/layout.tsx`](../app/layout.tsx). |
| Test harness behavior | [`tests/global-setup.js`](../tests/global-setup.js) (build + server ordering), [`playwright.config.js`](../playwright.config.js). |
| Deploy / headers | [`netlify.toml`](../netlify.toml), [`deploys.md`](deploys.md). |

## Testing Model

One Playwright suite, five kinds of test, **90 total**:

| Kind | File | What it proves |
|---|---|---|
| Smoke | `smoke-next.spec.js`, `smoke-interaction.spec.js` | Each route loads without console errors; tab switch and lightbox work. |
| Visual gate | `visual-baseline.spec.js` | 40 snapshots = 5 pages x 4 breakpoints (360/768/1024/1440) x 2 themes. A real compare gate, not capture-only. |
| Bubble engine | `bubbles-exclusion.spec.js` | 10 specs. Motion-enabled - the visual gate runs under `prefers-reduced-motion`, where the engine creates nothing. **Contains the suite's one known flake:** Contact form @ 1440px, ~1 run in 3 (Entry 118). |
| Gallery expand | `gallery-expand.spec.js` | 17 specs. The other motion-enabled file. Covers the expand interaction, which the visual gate cannot see at all: it only ever captures the collapsed grid, under reduced motion. |
| Set strips | `mistrust-sets.spec.js` | 8 specs. Holds the committed `set-N.webp` to `frame-geometry.json`, checking every slide appears at its own manifest offset rather than only that the total width is right. Covers a blind spot: the app renders its own CSS mosaic from individual slides, so a broken strip is invisible to every other test. |

Smallest useful commands: `npx playwright test -g "<name>"` for one case,
`npx tsc --noEmit` for types, `npm run css:build` for the legacy stylesheet
(expect byte-identical output if nothing changed).

Three harness facts that are easy to break and cost a cycle each:

- `next build` runs in **`tests/global-setup.js`**, not in `webServer.command` -
  `reuseExistingServer` skips the command when the port is held, which made the
  suite grade a stale `out/`.
- Reduced motion is applied with `page.emulateMedia()`, **not**
  `test.use({ reducedMotion })` - the declarative option is silently ignored on
  Playwright 1.61.1.
- `bubbles-exclusion.spec.js` runs `mode: 'serial'` **on purpose**. The engine
  integrates per frame, so concurrent runs starve rAF and leave bubbles grazing
  zone edges, which reads exactly like a real regression.
- That spec's helpers **skip bubbles at `opacity <= 0.05`**, and must keep doing
  so. `resolveZoneCollisions` ignores bubbles flagged `_relocating` - the
  deadlock rescue sets it for ~560ms while a trapped bubble fades out and
  respawns, and for the first ~260ms the element is still at its old position
  inside the zone. Measuring it there reports a fading bubble as coverage
  (Entry 115). Visible bubbles are still held to exactly zero overlap.
- Do not rename the `chromium` project - the 40 baselines are suffixed
  `-chromium-win32`.

To accept an intentional visual change: `npm test -- --update-snapshots`, review
the regenerated PNGs, then **re-run the full suite until green twice in a row**.
A bulk update can silently skip files, and a bad baseline passes on the update run
and only surfaces later as a phantom regression.

## Conventions

- **`--brand-content-max` (1400px) is the single source of truth** for content
  width. Never hardcode `max-w-[1400px]`; use `max-w-(--brand-content-max)`.
- **The container carries the width, children carry the gutter** - a flat 24px
  (`px-6`). `main` has **no** horizontal padding on purpose; re-adding it
  compounds with any nested `.brand-container`.
- Content needing a readable measure (prose, form fields) caps *inside* the shared
  container and stays **left-aligned**. `mx-auto` on such a block breaks the
  shared edge.
- Focus-visible contract: **`var(--brand-accent)`**, 2px, on all interactive
  elements.
- `dark:` is keyed to `[data-theme="dark"]`, not `prefers-color-scheme`.
- Generated `style.css` is tracked and committed. Rebuild after any class change.
- Class names referenced by JS must stay in markup even when styled by utilities:
  `.project-card`, `.about-box`, `.bubble-exclude`, `.hero-logo`, `.hero-name`,
  `.hero-sub`, `.project-tab`, `.gallery-item`, `.wip-notice`, `.brand-nav*`,
  `.brand-footer*`.
- Agent pointer files (`CLAUDE.md`, `CODEX.md`, `GEMINI.md`, `CURSOR.md`,
  `KILO.md`, `COPILOT.md`, `BLACKBOX.md`) all redirect to `AGENTS.md`. Update
  `AGENTS.md`, not the pointers.

## Agent Guidance

**Read first:** [`../AGENTS.md`](../AGENTS.md) for process and branch policy, this
file for structure, [`../TODO.md`](../TODO.md) for open work,
[`../LOGBOOK.md`](../LOGBOOK.md) (newest-first) for why things are the way they are.

**Do not touch without reading the relevant LOGBOOK entry:**

- `netlify.toml`'s `[build] ignore` rule - a bare `git diff --quiet` with unset
  refs exits 0 and would silently skip **every** build forever.
- `tests/global-setup.js` server ordering and its `stdio: 'ignore'` - piping
  `serve`'s request log deadlocked its event loop once already.
- The `mode: 'serial'` line in `bubbles-exclusion.spec.js`.
- `main`'s zero horizontal padding in `src/css/site.css`.

**Footguns, all of which have actually bitten:**

- **Renaming or retagging an element silently drops it out of the bubble exclusion
  zones.** They match by selector, so an element stops being avoided the moment it
  stops matching - no error, and the visual gate cannot see it (reduced motion =
  no bubbles). This has happened three times: the hero logo when inlined to
  `<svg>`, the Projects rail when restyled `.brand-btn` -> `.project-tab`, and the
  Contact form which never matched at all. When you rename a UI element, check
  `DEFAULT_EXCLUSIONS` in the same change and add a bubble spec case.
- **The theme toggle carries both `id="theme-toggle"` and
  `class="brand-theme-toggle"`, and the ID block wins.** Editing only the class is
  a no-op.
- **The nav has no spare room at 360px.** Fitting the fourth label took the lower
  bound of every clamp plus a 44px toggle cap. A fifth label needs a drawer.
- **`scripts/bubbles.js` and `public/scripts/bubbles.js` are duplicates.** Edit
  the former and copy, or the built site keeps the old behavior.
- **The visual gate cannot catch cross-page misalignment** - it grades each page
  against its own past self, so a permanently misaligned page stays green. Use
  `node scripts/measure-content-widths.js` (exits non-zero on divergence).
- **A green suite is not proof a small visual change shipped.** Tolerance is an
  absolute `maxDiffPixels: 500` floor; verify small changes by cropping the
  baseline or probing computed styles.
- Never write a Tailwind class name into a tracked `.md` expecting it to be inert
  (see the `@source not` rules above).

**Regenerate, don't hand-edit:** `images/og-default.png`
(`node scripts/generate-og-image.js`, needs `npm run dev`), the Mistrust webp
assets (`node scripts/generate-mistrust-assets.js [--all]`), and `style.css`
(`npm run css:build`).

## Appendix - Structural Index

A [repomix](https://repomix.com) compressed digest was used to seed this map. It
is **not committed** - `.shxdowmap/` is gitignored. Regenerate ad hoc with:

```bash
npx repomix --compress --output .shxdowmap/seed.txt
```

Freshness is enforced by the `shxdowmap` `post-commit` gate: it fingerprints
code-file paths, code-dir set, and declared-manifest blob SHAs, and marks
`.shxdowmap/stale` on structural drift. Ordinary edits to function bodies do not
trip it; adds, removes, moves, renames, and dependency changes do.

```bash
shxdowmap status          # fresh / STALE / no baseline
shxdowmap refresh --auto  # deterministic rebuild of the generated blocks
```
