# Kilo — Agent Pointer

> **Canonical agent docs live in [`AGENTS.md`](./AGENTS.md).** Read that first.

This file exists so Kilo finds project-specific instructions when opening this repo.

## Kilo-specific notes
- Kilo global config lives at `~/.config/kilo/kilo.jsonc`.
- Project-level config is at `.kilo/kilo.json`.
- This is the current active agent for this workspace.
