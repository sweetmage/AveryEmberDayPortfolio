# Claude Code — Agent Pointer

> **Canonical agent docs live in [`AGENTS.md`](./AGENTS.md).** Read that first.

This file exists so Claude Code finds project-specific instructions when opening this repo.

## Claude-specific notes
- Claude has access to `~/.claude/memory/` for persistent context (see `~/.claude/CLAUDE.md`).
- When updating global conventions that affect this repo, mirror changes into `AGENTS.md`.
