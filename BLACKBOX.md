# Blackbox — Agent Pointer

> **Canonical agent docs live in [`AGENTS.md`](./AGENTS.md).** Read that first.

This file exists so Blackbox finds project-specific instructions when opening this repo.

## Blackbox-specific notes
- Blackbox is configured as a provider in `~/.config/kilo/kilo.jsonc` (currently disabled).
- When updating global conventions that affect this repo, mirror changes into `AGENTS.md`.
