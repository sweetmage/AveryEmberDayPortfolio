# Cursor — Agent Pointer

> **Canonical agent docs live in [`AGENTS.md`](./AGENTS.md).** Read that first.

This file exists so Cursor finds project-specific instructions when opening this repo.

## Cursor-specific notes
- Cursor skills are shared from `~/.cursor/skills` and `~/.agents/skills`.
- When updating global conventions that affect this repo, mirror changes into `AGENTS.md`.
